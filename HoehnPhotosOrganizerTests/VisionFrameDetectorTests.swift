import XCTest
@testable import HoehnPhotosOrganizer

// VisionFrameDetector, FrameDetectionTask, and VisionModelProvider were removed from the codebase.
// These tests need to be rewritten or removed in a future phase.

final class VisionFrameDetectorTests: XCTestCase {

    func testPlaceholder() throws {
        throw XCTSkip("Deferred: VisionFrameDetector, FrameDetectionTask types no longer exist")
    }
}
