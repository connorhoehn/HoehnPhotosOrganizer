import XCTest
@testable import HoehnPhotosOrganizer

final class PrintLabViewModelTests: XCTestCase {

    // MARK: - PRINT-CANVAS-4: Page setup reads back paper size

    func test_pageSetup_landscapeUpdatesWidthAndHeight() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    func test_pageSetup_portraitSetsIsPortraitTrue() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    func test_pageSetup_cancelDoesNotChangeDimensions() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    // MARK: - PRINT-CANVAS-7: Aspect-ratio locked scaling

    func test_aspectLock_enteringWidthRecalculatesHeight() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    func test_aspectLock_disabledAllowsFreeformResize() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    // MARK: - PRINT-CANVAS-1: Library picker populates from catalog

    func test_addCanvasImage_appendsToCanvasImages() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    func test_removeCanvasImage_removesFromCanvasImages() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }

    func test_canvasImages_initiallyEmpty() throws {
        throw XCTSkip("Phase 9 — PrintLabViewModel not yet implemented")
    }
}
